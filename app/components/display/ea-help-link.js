import Component from '@glimmer/component';

export default class DisplayEaHelpLinkComponent extends Component {
  popperOptions = { modifiers: { preventOverflow: { escapeWithReference: false } } }
}
