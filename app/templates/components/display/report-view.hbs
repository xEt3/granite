<thead>
  <tr>
    {{#each this.headers as |header|}}
      <th>{{header}}</th>
    {{/each}}
  </tr>
</thead>
<tbody>
  {{#each this.bodyData as |row|}}
    {{display/report-view-row row this.headers.length}}
  {{/each}}
</tbody>
