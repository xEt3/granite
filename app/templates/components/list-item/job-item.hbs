{{#if hasBlock}}
  {{yield model}}
{{else}}
  <div class="header">{{model.name}}</div>
  <div class="meta">
    <span class="ui small text right floated">Created: {{time model.created}}{{#if model.creator}}by {{model.creator.fullName}}{{/if}}</span>
  </div>
  <div class="extra">
  </div>
{{/if}}
