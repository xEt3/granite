{{#if hasBlock}}
  {{yield model}}
{{else}}
  <div class="content">
    <div class="header">{{model.title}}</div>
    <div class="meta">
      <span class="ui small text right floated">{{time employee.created}}</span>
    </div>
    <div class="extra">
    </div>
  </div>
{{/if}}
