<Messaging::MessageThread
  @messages={{this.model.messages}}
  @thread={{this.model.thread}}
  @retrievalMax={{this.retrievalMax}}
  @onMessage={{this.sendMessage}}
  @onScrollback={{this.scrollback}}
/>
