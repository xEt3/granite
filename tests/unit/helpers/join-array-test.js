import { joinArray } from 'granite/helpers/join-array';
import { module, test } from 'qunit';

module('Unit | Helper | join array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = joinArray([42]);
  assert.ok(result);
});
