import { relativeGreetingTime } from 'granite/helpers/relative-greeting-time';
import { module, test } from 'qunit';

module('Unit | Helper | relative greeting time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = relativeGreetingTime([42]);
  assert.ok(result);
});
