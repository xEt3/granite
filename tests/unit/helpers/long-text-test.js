
import { longText } from 'granite/helpers/long-text';
import { module, test } from 'qunit';

module('Unit | Helper | long text');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = longText([ 42 ]);
  assert.ok(result);
});

