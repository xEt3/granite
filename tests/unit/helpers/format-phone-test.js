import { formatPhone } from 'granite/helpers/format-phone';
import { module, test } from 'qunit';

module('Unit | Helper | format phone');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatPhone([42]);
  assert.ok(result);
});
