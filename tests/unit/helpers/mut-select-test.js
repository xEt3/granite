import { mutSelect } from 'granite/helpers/mut-select';
import { module, test } from 'qunit';

module('Unit | Helper | mut select');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mutSelect([42]);
  assert.ok(result);
});
