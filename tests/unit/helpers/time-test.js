import { time } from 'granite/helpers/time';
import { module, test } from 'qunit';

module('Unit | Helper | time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = time([42]);
  assert.ok(result);
});
