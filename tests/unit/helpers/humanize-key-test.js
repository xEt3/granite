import { humanizeKey } from 'granite/helpers/humanize-key';
import { module, test } from 'qunit';

module('Unit | Helper | humanize key');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = humanizeKey([42]);
  assert.ok(result);
});
