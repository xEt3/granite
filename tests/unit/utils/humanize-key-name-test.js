import humanizeKeyName from 'granite/utils/humanize-key-name';
import { module, test } from 'qunit';

module('Unit | Utility | humanize key name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = humanizeKeyName();
  assert.ok(result);
});
