import resolveForTypeKey from 'granite/utils/resolve-for-type-key';
import { module, test } from 'qunit';

module('Unit | Utility | resolve for type key');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resolveForTypeKey();
  assert.ok(result);
});
