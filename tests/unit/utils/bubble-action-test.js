import bubbleAction from 'granite/utils/bubble-action';
import { module, test } from 'qunit';

module('Unit | Utility | bubble action');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bubbleAction();
  assert.ok(result);
});
