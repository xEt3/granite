import titleCase from 'granite/utils/title-case';
import { module, test } from 'qunit';

module('Unit | Utility | title case');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = titleCase();
  assert.ok(result);
});
