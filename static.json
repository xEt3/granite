{
  "https_only": true,
  "max_body_size": "20M",
  "proxies": {
    "/api/v1/": {
      "origin": "${API_URL}"
    },
    "/socket.io/": {
      "origin": "${WS_URL}",
      "allow_websocket_upgrade": true
    }
  }
}
